apiVersion : networking.k8s.io/v1
kind: ingress
metadata:
  name: linkding
spec:
  ingressClassName: traefik
  rules:
    - host: booky.jimmyalabs.org
      http:
        paths:
          - backend:
              service:
                name: linkding
                port:
                  number: 9090
            path: /
            pathType: Prefix
